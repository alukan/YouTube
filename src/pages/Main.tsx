import React from 'react';
import SearchComponent from '../components/SearchComponent';

const VideoPreviewsPage: React.FC = () => {

  return (
    <SearchComponent />
  );
};

export default VideoPreviewsPage;
